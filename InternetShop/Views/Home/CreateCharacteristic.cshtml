@model InternetShop.Models.CharactContainer

<div>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div>
            @Html.LabelFor(model => model.character.Name)
            <p>@Html.EditorFor(model => model.character.Name)</p>
        </div>

        <div>
            @Html.LabelFor(model => model.character.Description)
            <p>@Html.EditorFor(model => model.character.Description)</p>
        </div>
        <div>
        <p><input type="submit" value="Добавить" /></p>
            </div>
    }
</div>
